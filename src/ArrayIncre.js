import React from 'react'

 function ArrayIncre() {
    function App() {
        const numbers = [1, 2, 3, 4, 5];
        const doubled = numbers.map((number) => number * 2);
        console.log(doubled)
    return (
        <div>
            <h1>hello</h1>
        </div>
    )
    }
    }
    export default ArrayIncre;